mysql:
 image: mysql
 environment:
  MYSQL_ROOT_PASSWORD: root
  MYSQL_USER: user
  MYSQL_PASSWORD: pass
  MYSQL_DATABASE: boot
 volumes:
  - /var/lib/mysql
web:
 build: .
 links:
  - mysql
 ports:
 - 8080:8080

